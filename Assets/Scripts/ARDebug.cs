using UnityEngine;
using UnityEngine.XR.ARFoundation;

public class ARDebug : MonoBehaviour
{
    public ARSession session;

    void Update()
    {
        //Debug.Log(session.subsystem?.running);
    }
}
